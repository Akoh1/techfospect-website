# netlify.toml
# Tells Netlify where to find your static files and serverless functions.

[build]
  publish   = "."                  # Serve files from the root folder
  functions = "netlify/functions"  # Where serverless functions live

[functions]
  node_bundler = "esbuild"         # Use esbuild bundler (required for Node 18)
